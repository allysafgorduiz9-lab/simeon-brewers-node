<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Simeon Cafe | Premium Coffee Shop</title>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;600&family=Playfair+Display:wght@700&display=swap" rel="stylesheet">
    <style>
        :root {
            --bg-dark: #0f0f0f;
            --card-dark: #1a1a1a;
            --accent-gold: #C6A664;
            --accent-hover: #e0c285;
            --text-light: #ffffff;
            --text-dim: #b3b3b3;
            --status-green: #27ae60;
            --danger: #e74c3c;
            --fb-blue: #1877F2;
        }
        * { box-sizing: border-box; transition: all 0.3s ease; }
        body { font-family: 'Poppins', sans-serif; margin: 0; background-color: var(--bg-dark); color: var(--text-light); scroll-behavior: smooth; }
        
        /* Header Styles */
        header { background-color: rgba(15, 15, 15, 0.98); padding: 15px 8%; display: flex; justify-content: space-between; align-items: center; position: sticky; top: 0; z-index: 1000; border-bottom: 1px solid rgba(255,255,255,0.05); backdrop-filter: blur(10px); }
        .logo-section { display: flex; align-items: center; }
        .logo-section img { border-radius: 8px; margin-right: 15px; border: 1px solid var(--accent-gold); }
        .brand-name h1 { font-family: 'Playfair Display', serif; font-size: 20px; margin: 0; letter-spacing: 1px; }
        .brand-name p { font-size: 9px; margin: 0; color: var(--accent-gold); font-weight: 600; text-transform: uppercase; }
        .status-pill { background: var(--status-green); color: white; padding: 3px 10px; border-radius: 4px; font-size: 9px; font-weight: bold; margin-top: 5px; display: inline-block; }
        .status-pill.closed { background: var(--danger); }
        
        /* Navigation */
        nav { display: flex; align-items: center; gap: 25px; }
        nav a { color: var(--text-light); text-decoration: none; font-size: 13px; font-weight: 600; }
        nav a:hover { color: var(--accent-gold); }
        .nav-actions { display: flex; align-items: center; gap: 12px; position: relative; }
        .login-btn, .cart-btn { background: rgba(255,255,255,0.03); border: 1px solid rgba(255,255,255,0.1); padding: 8px 14px; border-radius: 10px; display: flex; align-items: center; gap: 8px; cursor: pointer; color: var(--text-light); text-decoration: none; }
        .login-btn:hover, .cart-btn:hover { border-color: var(--accent-gold); background: rgba(198, 166, 100, 0.05); }
        
        /* Orders Dropdown */
        .orders-dropdown {
            position: absolute;
            top: 100%;
            right: 0;
            width: 320px;
            max-height: 400px;
            overflow-y: auto;
            background: var(--card-dark);
            border: 1px solid var(--border);
            border-radius: 15px;
            box-shadow: 0 10px 30px rgba(0,0,0,0.5);
            z-index: 9999;
            display: none;
            margin-top: 10px;
        }
        .orders-dropdown.show { display: block; }
        .orders-dropdown-header {
            padding: 15px;
            border-bottom: 1px solid var(--border);
            font-weight: 600;
            color: var(--accent-gold);
            text-align: center;
        }
        .order-item-mini {
            padding: 15px;
            border-bottom: 1px solid rgba(255,255,255,0.1);
            cursor: pointer;
            transition: all 0.3s;
        }
        .order-item-mini:hover { background: rgba(198,166,100,0.1); }
        .order-item-mini:last-child { border-bottom: none; }
        .order-mini-number { font-weight: 700; color: var(--accent-gold); font-size: 14px; }
        .order-mini-date { font-size: 11px; color: var(--text-dim); margin-top: 3px; }
        .order-mini-total { font-weight: 600; float: right; }
        .order-mini-status {
            display: inline-block;
            padding: 2px 8px;
            border-radius: 6px;
            font-size: 10px;
            font-weight: 600;
            margin-top: 5px;
        }
        .status-pending { background: rgba(241,196,15,0.2); color: #f39c12; }
        .status-preparing { background: rgba(52,152,219,0.2); color: #3498db; }
        .status-completed { background: rgba(39,174,96,0.2); color: #27ae60; }
        .no-orders { padding: 30px; text-align: center; color: var(--text-dim); }
        
        /* Hero Section */
        .hero { height: 65vh; display: flex; flex-direction: column; justify-content: center; align-items: center; text-align: center; background: linear-gradient(rgba(0,0,0,0.7), rgba(0,0,0,0.9)), url('https://images.unsplash.com/photo-1495474472287-4d71bcdd2085?auto=format&fit=crop&q=80&w=1920'); background-size: cover; background-position: center; padding: 0 10%; }
        .hero h2 { font-family: 'Playfair Display', serif; font-size: clamp(32px, 5vw, 52px); margin-bottom: 15px; }
        .hero h2 span { color: var(--accent-gold); }
        .hero p { color: var(--text-dim); line-height: 1.8; max-width: 650px; font-weight: 300; }
        .hero .cta-btn { display: inline-block; margin-top: 25px; padding: 14px 35px; background: var(--accent-gold); color: #000; text-decoration: none; border-radius: 12px; font-weight: 600; }
        .hero .cta-btn:hover { background: var(--accent-hover); transform: scale(1.05); }
        
        /* Closed Banner */
        .store-closed-banner { background: var(--danger); color: white; text-align: center; padding: 15px; font-weight: 600; font-size: 16px; }
        
        /* Menu Container */
        .menu-container { padding: 80px 8%; }
        .search-container { max-width: 500px; margin: 0 auto 40px; position: relative; }
        .search-bar { width: 100%; padding: 16px 20px; padding-left: 50px; background: var(--card-dark); border: 1px solid rgba(255,255,255,0.1); border-radius: 14px; color: var(--text-light); font-size: 16px; font-family: inherit; }
        .search-bar:focus { outline: none; border-color: var(--accent-gold); }
        .search-icon { position: absolute; left: 18px; top: 50%; transform: translateY(-50%); color: var(--text-dim); }
        .section-title { text-align: center; margin-bottom: 50px; }
        .section-title h3 { color: var(--accent-gold); letter-spacing: 3px; font-size: 14px; text-transform: uppercase; }
        
        /* Product Grid */
        .product-grid { display: grid; grid-template-columns: repeat(auto-fill, minmax(280px, 1fr)); gap: 30px; }
        .product-card { background: var(--card-dark); border: 1px solid rgba(255,255,255,0.05); border-radius: 20px; padding: 25px; text-align: center; }
        .product-card:hover { transform: translateY(-8px); border-color: var(--accent-gold); box-shadow: 0 10px 30px rgba(0,0,0,0.5); }
        .product-card.unavailable { opacity: 0.5; }
        .product-img { width: 100%; height: 220px; object-fit: cover; border-radius: 15px; margin-bottom: 20px; }
        .product-name { font-family: 'Playfair Display', serif; font-size: 22px; margin: 10px 0; }
        .product-price { color: var(--accent-gold); font-size: 18px; font-weight: 600; margin-bottom: 10px; }
        
        /* Badges & Buttons */
        .availability-badge { display: inline-block; padding: 4px 10px; border-radius: 8px; font-size: 11px; font-weight: 600; margin-bottom: 15px; }
        .badge-available { background: rgba(39,174,96,0.2); color: #27ae60; }
        .badge-unavailable { background: rgba(231,76,60,0.2); color: #e74c3c; }
        .btn-add { background: var(--accent-gold); color: #000; border: none; padding: 14px; border-radius: 12px; width: 100%; font-weight: 600; cursor: pointer; }
        .btn-add:hover { background: var(--accent-hover); transform: scale(1.02); }
        .btn-disabled { background: #2a2a2a; color: #555; cursor: not-allowed; width: 100%; padding: 14px; border-radius: 12px; border: none; }
        .qty-input { width: 60px; background: #252525; color: white; border: 1px solid #444; padding: 12px; border-radius: 12px; text-align: center; }
        
        /* Pagination */
        .pagination { display: flex; justify-content: center; align-items: center; gap: 20px; margin-top: 50px; }
        .nav-btn { padding: 10px 20px; border: 1px solid rgba(255,255,255,0.1); border-radius: 8px; background: rgba(255,255,255,0.03); color: var(--accent-gold); text-decoration: none; font-weight: 600; font-size: 14px; display: flex; align-items: center; gap: 8px; }
        .nav-btn:hover:not(.disabled) { border-color: var(--accent-gold); background: rgba(198, 166, 100, 0.1); transform: translateY(-2px); }
        .nav-btn.disabled { pointer-events: none; opacity: 0.3; color: var(--text-dim); }
        
        /* Footer */
        footer { background: #000; padding: 80px 8% 30px; border-top: 1px solid rgba(255,255,255,0.05); margin-top: 80px; }
        .footer-grid { display: grid; grid-template-columns: repeat(auto-fit, minmax(250px, 1fr)); gap: 50px; margin-bottom: 50px; }
        .footer-col h4 { color: var(--accent-gold); margin-bottom: 25px; font-size: 16px; font-family: 'Playfair Display', serif; }
        .footer-col p, .footer-col a { color: var(--text-dim); font-size: 14px; text-decoration: none; line-height: 1.8; display: block; }
        .footer-col a:hover { color: var(--accent-gold); }
        .footer-bottom { text-align: center; border-top: 1px solid #1a1a1a; padding-top: 30px; font-size: 11px; color: #444; letter-spacing: 1px; }
        .fb-btn { background: var(--fb-blue); color: white; padding: 10px 20px; border-radius: 8px; display: inline-flex; align-items: center; gap: 10px; font-weight: 600; margin-top: 15px; text-decoration: none; }
        
        /* Receipt Modal */
        .receipt-modal {
            position: fixed; top: 0; left: 0; width: 100%; height: 100%;
            background: rgba(0,0,0,0.95); z-index: 10000; display: none;
            align-items: center; justify-content: center; backdrop-filter: blur(10px);
        }
        .receipt-card {
            background: var(--card-dark); width: 90%; max-width: 420px; border-radius: 20px;
            padding: 35px; border: 3px solid var(--accent-gold); position: relative;
            max-height: 90vh; overflow-y: auto;
        }
        .close-btn {
            position: absolute; top: 15px; right: 20px; background: none;
            border: none; color: white; font-size: 28px; cursor: pointer; padding: 0;
        }
        .order-number {
            background: var(--accent-gold); color: #000; padding: 15px 30px;
            border-radius: 35px; font-weight: bold; font-size: 22px;
            margin: 0 auto 25px; display: block; text-align: center; letter-spacing: 2px;
        }
        .receipt-item {
            display: flex; justify-content: space-between; padding: 12px 0;
            border-bottom: 1px solid rgba(255,255,255,0.1); font-size: 14px;
        }
        .receipt-total {
            font-size: 26px; font-weight: bold; color: var(--accent-gold);
            margin: 20px 0 15px; padding-top: 15px; border-top: 2px solid var(--accent-gold);
            display: flex; justify-content: space-between;
        }
        .status-badge {
            display: inline-block; padding: 10px 25px; border-radius: 30px;
            font-weight: 700; font-size: 14px; margin: 15px auto; text-align: center;
        }
        .status-pending { background: linear-gradient(45deg, #f39c12, #e67e22); color: white; }
        .btn-back-home {
            background: var(--status-green); color: white; border: none;
            padding: 18px; border-radius: 12px; width: 100%;
            font-weight: 700; font-size: 16px; cursor: pointer; margin-top: 15px;
        }
        
        /* Responsive */
        @media (max-width: 768px) {
            header { padding: 15px 5%; }
            nav { gap: 15px; }
            nav a { font-size: 11px; }
            .menu-container { padding: 50px 5%; }
            .hero { height: 50vh; }
            .product-grid { grid-template-columns: repeat(auto-fill, minmax(220px, 1fr)); gap: 20px; }
            .orders-dropdown { width: 280px; right: -50px; }
        }
    </style>
</head>
<body>
    <!-- Header with Navigation -->
    <header>
        <div class="logo-section">
            <img src="/images/Simeon Cafe.jpg" width="45" alt="Simeon Logo" onerror="this.style.display='none'">
            <div class="brand-name">
                <h1>SIMEON CAFE</h1>
                <p>Premium Coffee Shop</p>
                <span class="status-pill <%= storeStatus === 'CLOSED' ? 'closed' : '' %>"><%= storeStatus %></span>
            </div>
        </div>
        <nav>
            <a href="/">HOME</a>
            <a href="#menu">MENU</a>
            <a href="#about">ABOUT</a>
            <div class="nav-actions">
                <a href="/login" class="login-btn" title="Staff Login">
                    <i class="fas fa-user-circle"></i>
                </a>
                <div class="cart-btn" onclick="toggleOrders()" title="My Orders" style="position:relative;">
                    <i class="fas fa-receipt" style="color: var(--accent-gold);"></i>
                    <span id="order-count" style="display:none;">0</span>
                </div>
                <div class="cart-btn" onclick="checkout()" title="View Cart">
                    <i class="fas fa-shopping-cart" style="color: var(--accent-gold);"></i>
                                        <span id="cart-count">0</span>
                </div>
            </div>
        </nav>
    </header>

    <!-- Orders Dropdown -->
    <div class="orders-dropdown" id="ordersDropdown">
        <div class="orders-dropdown-header">
            <i class="fas fa-receipt"></i> My Orders
        </div>
        <div id="ordersListContainer">
            <div class="no-orders">
                <i class="fas fa-receipt" style="font-size:30px;opacity:0.3;"></i>
                <p style="margin-top:10px;">No orders yet</p>
            </div>
        </div>
    </div>

    <!-- Hero Section -->
    <section class="hero">
        <h2>Welcome to <span>Simeon Cafe</span></h2>
        <p>Experience the finest handcrafted coffee and treats in Silago.</p>
        <a href="#menu" class="cta-btn">View Menu</a>
    </section>

    <!-- Store Closed Banner -->
    <% if (storeStatus === 'CLOSED') { %>
    <div class="store-closed-banner">
        <i class="fas fa-exclamation-circle"></i> STORE IS CURRENTLY CLOSED - Online ordering is not available
    </div>
    <% } %>

    <!-- Menu Section -->
    <div class="menu-container" id="menu">
        <div class="section-title">
            <h3>Discover Our Favorites</h3>
        </div>
        
        <!-- Search Bar -->
        <div class="search-container">
            <i class="fas fa-search search-icon"></i>
            <input type="text" class="search-bar" id="searchInput" placeholder="Search menu..." onkeyup="searchProducts()">
        </div>
        
        <!-- Product Grid -->
        <div class="product-grid" id="productGrid">
            <% 
            var itemsPerPage = 8;
            var currentPage = page || 1;
            var start = (currentPage - 1) * itemsPerPage;
            var end = start + itemsPerPage;
            var paginatedProducts = products.slice(start, end);
            var totalPages = Math.ceil(products.length / itemsPerPage);
            %>
            <% paginatedProducts.forEach(function(p) { %>
            <div class="product-card <%= p.active == 0 ? 'unavailable' : '' %>" data-name="<%= p.name.toLowerCase() %>">
                <img src="/images/<%= p.image %>" class="product-img" onerror="this.src='https://images.unsplash.com/photo-1509042239860-f550ce710b93?q=80&w=400'" alt="<%= p.name %>">
                <div class="product-name"><%= p.name %></div>
                <div class="product-price">₱<%= parseFloat(p.price_1).toFixed(2) %></div>
                <span class="availability-badge <%= p.active == 1 ? 'badge-available' : 'badge-unavailable' %>">
                    <%= p.active == 1 ? 'Available' : 'Unavailable' %>
                </span>
                <div style="display: flex; gap: 10px; align-items: center; justify-content: center;">
                    <input type="number" value="1" min="1" id="qty-<%= p.id %>" class="qty-input" <%= (storeStatus === 'CLOSED' || p.active == 0) ? 'disabled' : '' %>>
                    <button class="<%= (storeStatus === 'OPEN' && p.active == 1) ? 'btn-add' : 'btn-disabled' %>"
                        data-id="<%= p.id %>"
                        data-name="<%= p.name %>"
                        data-price="<%= p.price_1 %>"
                        onclick="addToCart(this)"
                        <%= (storeStatus === 'CLOSED' || p.active == 0) ? 'disabled' : '' %>>
                        <%= storeStatus === 'CLOSED' ? 'CLOSED' : (p.active == 1 ? 'ADD TO CART' : 'Unavailable') %>
                    </button>
                </div>
            </div>
            <% }); %>
        </div>
        
        <!-- Pagination -->
        <% if (totalPages > 1) { %>
        <div class="pagination">
            <a href="/?page=<%= currentPage - 1 %>" class="nav-btn <%= currentPage <= 1 ? 'disabled' : '' %>">
                <i class="fas fa-arrow-left"></i> Previous
            </a>
            <span style="font-size: 14px; color: var(--accent-gold); font-weight: bold;">
                Page <%= currentPage %> of <%= totalPages %>
            </span>
            <a href="/?page=<%= currentPage + 1 %>" class="nav-btn <%= currentPage >= totalPages ? 'disabled' : '' %>">
                Next <i class="fas fa-arrow-right"></i>
            </a>
        </div>
        <% } %>
    </div>

    <!-- About Section -->
    <section id="about" style="padding: 80px 8%; background: #151515;">
        <div class="section-title">
            <h3>Our Story</h3>
            <h2 style="font-family: 'Playfair Display', serif; font-size: 32px; margin-bottom: 20px;">About <span style="color: var(--accent-gold);">Simeon Cafe</span></h2>
        </div>
        <div style="max-width: 800px; margin: 0 auto; text-align: center; color: var(--text-dim); line-height: 1.8;">
            <p>Welcome to Simeon Cafe, the premier coffee destination in Silago. We are dedicated to sourcing the finest beans from around the world and roasting them to perfection to bring you a truly exceptional cup of coffee.</p>
            <p>Our passion goes beyond just coffee – we create a warm and welcoming atmosphere where community comes first. Whether you're here for a quick grab-and-go or a relaxing afternoon, Simeon Cafe is your home away from home.</p>
            <div style="margin-top: 40px; display: flex; justify-content: center; gap: 30px; flex-wrap: wrap;">
                <div style="text-align: center;">
                    <i class="fas fa-mug-hot" style="font-size: 40px; color: var(--accent-gold); margin-bottom: 15px;"></i>
                    <h4 style="color: white; margin: 0;">Premium Coffee</h4>
                </div>
                <div style="text-align: center;">
                    <i class="fas fa-heart" style="font-size: 40px; color: var(--accent-gold); margin-bottom: 15px;"></i>
                    <h4 style="color: white; margin: 0;">Community Focused</h4>
                </div>
                <div style="text-align: center;">
                    <i class="fas fa-leaf" style="font-size: 40px; color: var(--accent-gold); margin-bottom: 15px;"></i>
                    <h4 style="color: white; margin: 0;">Sustainable Sourcing</h4>
                </div>
            </div>
        </div>
    </section>

    <!-- Footer -->
       <footer>
        <div class="footer-grid">
            <div class="footer-col">
                <h4>ABOUT</h4>
                <p>Simeon Cafe brings the finest beans to Silago for an unparalleled coffee experience. We are committed to quality, community, and exceptional service.</p>
                <a href="https://facebook.com" target="_blank" class="fb-btn">
                    <i class="fab fa-facebook-f"></i> Like us on Facebook
                </a>
            </div>
            
            <div class="footer-col">
                <h4>CONTACT</h4>
                <p><i class="fas fa-map-marker-alt"></i> Poblacion District 1, Silago, Southern Leyte - Front of SJAP Plaza</p>
                <p><i class="fas fa-phone"></i> +63 977 105 3180</p>
                <p><i class="fas fa-envelope"></i> hersanofritz@gmail.com</p>
            </div>
            
            <div class="footer-col">
                <h4>LOCATION</h4>
                <p><i class="fas fa-map-marker-alt"></i> Poblacion District 1, Silago, Southern Leyte</p>
                <a href="https://www.google.com/maps?q=10.523861,125.159028" target="_blank" style="background: var(--accent-gold); color: #000; padding: 12px 20px; border-radius: 8px; display: inline-flex; align-items: center; gap: 10px; font-weight: 600; text-decoration: none; margin-top: 10px;">
                    <i class="fas fa-directions"></i> Get Directions
                </a>
            </div>
            
            <div class="footer-col">
                <h4>OPENING HOURS</h4>
                <p>Mon - Sun: 9:00 AM - 5:00 PM</p>
            </div>
            
            <div class="footer-col">
                <h4>QUICK LINKS</h4>
                <a href="/">Home</a>
                <a href="#menu">Menu</a>
                <a href="/login">Staff Login</a>
                <a href="/terms">Terms & Conditions</a>
            </div>
        </div>
        
        <div class="footer-bottom">
            &copy; 2023 Simeon Cafe. All rights reserved. | Designed for Simeon Cafe
        </div>
    </footer>

    <!-- Receipt Modal -->
    <div class="receipt-modal" id="receiptModal">
        <div class="receipt-card">
            <button class="close-btn" onclick="closeReceipt()">&times;</button>

            <div class="order-number" id="orderNumber">#0001</div>

            <div style="background: rgba(255,255,255,0.05); padding: 15px; border-radius: 10px; margin-bottom: 20px;">
                <p style="margin: 0; font-size: 13px; color: var(--text-dim);">Customer</p>
                <p style="margin: 5px 0 0; font-weight: 600;" id="receiptCustomer">Name</p>
                <p style="margin: 5px 0 0; font-size: 13px; color: var(--text-dim);" id="receiptPhone">Phone</p>
            </div>

            <div id="receiptItems" style="margin: 25px 0;"></div>

            <div class="receipt-total">
                <span>TOTAL:</span>
                <span id="receiptTotal">₱0.00</span>
            </div>

            <div class="status-badge status-pending" id="orderStatus">PENDING</div>

            <p style="font-size: 14px; color: var(--text-dim); text-align: center; margin: 20px 0;">
                📋 Show this order number to staff<br>
                <small>Track your order status in real-time</small>
            </p>

            <button class="btn-back-home" onclick="window.location.href='/'">
                🏠 Back to Home
            </button>
        </div>
    </div>

    <!-- JavaScript -->
    <!-- JavaScript -->
<script>
    // 1. Add to Cart
    function addToCart(button) {
        const id = button.getAttribute('data-id');
        const name = button.getAttribute('data-name');
        const price = parseFloat(button.getAttribute('data-price'));
        const qtyInput = document.getElementById('qty-' + id);
        const quantity = parseInt(qtyInput.value);

        let cart = JSON.parse(localStorage.getItem('simeonCart')) || [];
        const existingItem = cart.find(item => item.id === id);
        
        if (existingItem) {
            existingItem.quantity += quantity;
        } else {
            cart.push({ id, name, price, quantity });
        }

        localStorage.setItem('simeonCart', JSON.stringify(cart));
        updateCartCount();
        
        const originalText = button.innerText;
        button.innerText = 'ADDED!';
        button.style.background = '#27ae60';
        button.style.color = 'white';
        
        setTimeout(() => {
            button.innerText = originalText;
            button.style.background = '';
            button.style.color = '';
        }, 1500);
    }

    // 2. Update Cart Count
    function updateCartCount() {
        const cart = JSON.parse(localStorage.getItem('simeonCart')) || [];
        const total = cart.reduce((sum, item) => sum + item.quantity, 0);
        document.getElementById('cart-count').innerText = total;
    }

    // 3. Checkout
    function checkout() {
        const cart = JSON.parse(localStorage.getItem('simeonCart')) || [];
        if (cart.length === 0) {
            alert('Your cart is currently empty!');
            return;
        }
        window.location.href = '/checkout';
    }

    // 4. Search Products
    function searchProducts() {
        const input = document.getElementById('searchInput');
        const filter = input.value.toLowerCase();
        const grid = document.getElementById('productGrid');
        const cards = grid.getElementsByClassName('product-card');

        for (let i = 0; i < cards.length; i++) {
            const name = cards[i].getAttribute('data-name');
            if (name.indexOf(filter) > -1) {
                cards[i].style.display = "";
            } else {
                cards[i].style.display = "none";
            }
        }
    }

    // 5. Toggle Orders Dropdown
    function toggleOrders() {
        const dropdown = document.getElementById('ordersDropdown');
        dropdown.classList.toggle('show');
        renderOrdersList();
    }

    // 6. Render Orders List with SYNCED status
    async function renderOrdersList() {
        const orders = JSON.parse(localStorage.getItem('orderHistory')) || [];
        const container = document.getElementById('ordersListContainer');
        const countBadge = document.getElementById('order-count');
        
        // Sync each order with database
        for (let i = 0; i < orders.length; i++) {
            try {
                const response = await fetch('/api/orders/status/' + orders[i].orderNumber);
                const result = await response.json();
                
                if (result.success) {
                    orders[i].status = result.status;
                }
            } catch (error) {
                console.log('Sync failed for order:', orders[i].orderNumber);
            }
        }
        localStorage.setItem('orderHistory', JSON.stringify(orders));
        
        const activeOrders = orders.filter(o => o.status !== 'Completed');
        if (activeOrders.length > 0) {
            countBadge.style.display = 'inline';
            countBadge.textContent = activeOrders.length;
        } else {
            countBadge.style.display = 'none';
        }
        
        if (orders.length === 0) {
            container.innerHTML = '<div class="no-orders"><i class="fas fa-receipt" style="font-size:30px;opacity:0.3;"></i><p style="margin-top:10px;">No orders yet</p></div>';
            return;
        }
        
        container.innerHTML = orders.map((order, index) => `
            <div class="order-item-mini" onclick="showOrderDetail(${index})">
                <div>
                    <span class="order-mini-number">#${order.orderNumber}</span>
                    <span class="order-mini-total">₱${order.totalAmount.toFixed(2)}</span>
                </div>
                <div class="order-mini-date">${new Date(order.date).toLocaleString()}</div>
                <span class="order-mini-status status-${(order.status || 'Pending').toLowerCase()}">${order.status || 'Pending'}</span>
            </div>
        `).join('');
    }

    // 7. Show Order Detail with SYNCED status
    async function showOrderDetail(index) {
        const orders = JSON.parse(localStorage.getItem('orderHistory')) || [];
        const order = orders[index];
        
        if (!order) return;
        
        // Fetch latest status from database
        try {
            const response = await fetch('/api/orders/status/' + order.orderNumber);
            const result = await response.json();
            
            if (result.success) {
                order.status = result.status;
                orders[index] = order;
                localStorage.setItem('orderHistory', JSON.stringify(orders));
            }
        } catch (error) {
            console.log('Could not fetch status');
        }
        
        // Display the receipt
        document.getElementById('orderNumber').textContent = '#' + order.orderNumber;
        document.getElementById('receiptCustomer').textContent = order.customerName;
        document.getElementById('receiptPhone').textContent = order.customerPhone || '-';
        document.getElementById('receiptTotal').textContent = '₱' + order.totalAmount.toFixed(2);
        document.getElementById('orderStatus').textContent = order.status || 'Pending';
        document.getElementById('orderStatus').className = 'status-badge status-' + (order.status || 'Pending').toLowerCase();
        
        document.getElementById('receiptItems').innerHTML = order.items.map(item => `
            <div class="receipt-item">
                <span>${item.name} (x${item.quantity})</span>
                <span>₱${(item.price * item.quantity).toFixed(2)}</span>
            </div>
        `).join('');
        
        document.getElementById('ordersDropdown').classList.remove('show');
        document.getElementById('receiptModal').style.display = 'flex';
    }

    function closeReceipt() {
        document.getElementById('receiptModal').style.display = 'none';
    }

    // Close dropdown when clicking outside
    document.addEventListener('click', (e) => {
        const dropdown = document.getElementById('ordersDropdown');
        if (dropdown && !dropdown.contains(e.target) && !e.target.closest('[onclick="toggleOrders()"]')) {
            dropdown.classList.remove('show');
        }
    });

    // Initialize
    window.onload = function() {
        updateCartCount();
        renderOrdersList();
    };
</script>

</body>
</html>