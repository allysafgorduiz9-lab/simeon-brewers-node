<!DOCTYPE html>
<html>
<head>
    <title>Simeon Admin - Active Orders</title>
    <style>
        .nav-item { color: rgba(255,255,255,0.7); text-decoration: none; padding: 12px; border-radius: 8px; transition: 0.3s; font-size: 14px; display: block; }
        .nav-item:hover, .active { background: rgba(255,255,255,0.1); color: white; }
        .card { background: white; padding: 25px; border-radius: 15px; box-shadow: 0 4px 10px rgba(0,0,0,0.05); }
        .status-badge { padding: 5px 12px; border-radius: 20px; font-size: 11px; font-weight: bold; }
        .pending { background: #ffeaa7; color: #d63031; }
        table { width: 100%; border-collapse: collapse; margin-top: 20px; }
        th { text-align: left; padding: 15px; background: #2D1B14; color: white; }
        td { padding: 15px; border-bottom: 1px solid #eee; }
    </style>
</head>
<body style="background: #F8F4F1; display: flex; margin: 0; font-family: 'Segoe UI', sans-serif;">

    <div style="width: 260px; background: #2D1B14; height: 100vh; color: white; padding: 25px; position: fixed; box-sizing: border-box;">
        <div style="text-align: center; margin-bottom: 30px;">
            <img src="/Simeon Cafe.jpg" style="width: 60px; border-radius: 50%; border: 2px solid #C6A664;">
            <h3 style="color: #C6A664; margin-top: 10px;">SIMEON BREWERS</h3>
        </div>
        <nav>
            <a href="/admin/orders" class="nav-item active">Active Orders</a>
            <a href="/admin/categories" class="nav-item">Manage Categories</a>
            <a href="/admin/menu" class="nav-item">Manage Menu</a>
            <a href="/admin/reports" class="nav-item">Daily Reports</a>
            <hr style="border: 0; border-top: 1px solid rgba(255,255,255,0.1); margin: 15px 0;">
            <a href="/" target="_blank" class="nav-item" style="color: #C6A664; text-align: center;">View Live Site ↗</a>
            <a href="/logout" class="nav-item" style="color: #ff7675;">Logout</a>
        </nav>
    </div>

    <div style="margin-left: 260px; padding: 40px; width: 100%;">
        <h2 style="color: #2D1B14;">Incoming Customer Orders</h2>
        
        <div class="card">
            <table>
                <thead>
                    <tr>
                        <th>Order ID</th>
                        <th>Customer</th>
                        <th>Items</th>
                        <th>Total</th>
                        <th>Status</th>
                        <th>Action</th>
                    </tr>
                </thead>
                <tbody>
                    <% if (orders.length > 0) { %>
                        <% orders.forEach(order => { %>
                            <tr>
                                <td>#<%= order.id %></td>
                                <td><%= order.customer_name %></td>
                                <td><%= order.items %></td>
                                <td style="font-weight: bold;">₱<%= order.total_price %></td>
                                <td><span class="status-badge pending"><%= order.status %></span></td>
                                <td>
                                    <button style="background: #27ae60; color: white; border: none; padding: 5px 10px; border-radius: 5px; cursor: pointer;">Complete</button>
                                </td>
                            </tr>
                        <% }); %>
                    <% } else { %>
                        <tr>
                            <td colspan="6" style="text-align: center; color: #888; padding: 40px;">No active orders right now.</td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
    </div>
</body>
</html>